# Xcode / XcodeGen
iosApp/Silo.xcodeproj/
*.xcworkspace/
xcuserdata/
*.xcuserstate
DerivedData/
iosApp/.deriveddata/

# SwiftPM / CocoaPods
.build/
iosApp/.spm-packages/
iosApp/Pods/

# Fastlane build outputs (IPAs, archives from the ipa_*_unsigned lanes)
build/
fastlane/README.md

# Local signing and secrets
iosApp/Signing/Local.xcconfig
# Entitlements variants for a contributor's own team, referenced from
# Local.xcconfig via the SILO_*_ENTITLEMENTS variables.
*.local.entitlements
.env
.env.*
!.env.example
*.p8
*.p12
*.mobileprovision
*.cer

# Ruby / Fastlane
vendor/bundle/
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/
fastlane/test_output/

# Local tooling state
# `.agents/skills` is a symlink to `.claude/skills` so non-Claude agents find the
# same files; everything else under .agents/ is per-developer scratch.
/.agents/*
!/.agents/skills
# Ignore everything in .claude/ except the committed, shared skills.
/.claude/*
!/.claude/skills/
.codex/
.cursor/
.sweetpad/
.worktrees/
.playwright-mcp/
.playwright-cli/
.superpowers/
buildServer.json

# Logs and OS files
*.log
logs/
output/
.DS_Store
Thumbs.db
.swiftpm/
docs/*
!docs/tvos-focus.md
!docs/mac-builder.md
!docs/release/*
